// Package frame encodes and decodes binary frames according to a
// user-defined Format. It handles packet boundaries, header metadata,
// and integrity validation. Payload structure is left to the schema
// package.
package frame
